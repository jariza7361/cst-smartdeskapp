{
  "name": "cst-smartdesk-infra",
  "version": "1.0.0",
  "main": "bin/cst-smartdesk.js",
  "scripts": {
    "build": "tsc",
    "deploy": "cdk deploy --require-approval never",
    "destroy": "cdk destroy --force"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.100.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}