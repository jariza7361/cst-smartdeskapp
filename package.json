{
  "name": "cst-smartdeskapp",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "e2e": "playwright test",
    "postinstall": "playwright install --with-deps"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@playwright/test": "^1.45.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "vitest": "^1.0.0"
  }
}
