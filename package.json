{
  "name": "cst-smartdeskapp",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vite --open /index.dev.html",
    "build": "vite build",
    "deploy": "npm run build && node scripts/aws-sync.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "preview:local": "vite preview --host 127.0.0.1 --port 4174 --strictPort",
    "preview:prod": "vite preview --open /index.html --port 4174",
    "preview:ci": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "parity:smoke:ci": "npm run build && (npm run preview:ci &) && npx wait-on http://127.0.0.1:4173 && BASE_URL=http://127.0.0.1:4173 node scripts/parity-smoke.mjs",
    "doctor": "node scripts/doctor.mjs",
    "parity:smoke": "node scripts/parity-smoke.mjs",
    "test": "vitest run",
    "e2e": "playwright test",
    "aws:build": "npm run build",
    "aws:deploy": "npm -C infra run deploy",
    "aws:sync": "node scripts/aws-sync.mjs",
    "aws:invalidate": "node scripts/aws-invalidate.mjs",
    "aws:url": "node scripts/aws-url.mjs"
  },
  "devDependencies": {
    "@eslint/js": "9.11.1",
    "@playwright/test": "1.46.1",
    "eslint": "^9.34.0",
    "express": "^4.21.2",
    "prettier": "3.3.3",
    "tesseract.js": "2.1.5",
    "tesseract.js-core": "2.2.0",
    "vite": "^7.1.3",
    "vitest": "^3.2.4",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "uuid": "^11.1.0",
    "vercel": "^41.0.2"
  }
}
