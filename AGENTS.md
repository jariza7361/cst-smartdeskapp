# AGENTS

- Run `npm test` to execute the test suite. If tests are missing, record the failure.
- Use `npx prettier -w` to format files before committing.
- Install dependencies via `./setup.sh` when setting up the environment.
